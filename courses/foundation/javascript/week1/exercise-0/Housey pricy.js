const peterPrice = 8 * 10 * 10 * 2.5 * 1000 + 100 * 300;
const peterPaid = 2500000;
console.log(
  peterPrice < peterPaid ? "Peter paid too much" : "Peter got a good deal"
);

const juliaPrice = 5 * 11 * 8 * 2.5 * 1000 + 70 * 300;
const juliaPaid = 1000000;
console.log(
  juliaPrice < juliaPaid ? "Julia paid too much" : "Julia got a good deal"
);
